import React from 'react';
import UseTitle from '../../Hooks/UseTitle';

const Destination = () => {
    UseTitle("Destination");
    return (
        <div>
            <h1 className='text-success'>This is destinatin Page</h1>
        </div>
    );
};

export default Destination;